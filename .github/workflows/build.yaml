name: build action
on:
  push:
    branches: [ mecab ]
jobs:
  build_web:
    name: 編譯程式
    runs-on: windows-2019
    steps:

      - name: 下載必要檔案
        run: |
          echo $ErrorActionPreference
      - name: 結果
        uses: actions/upload-artifact@v3
        with:
          name: result
          path: Textractor
      
      
